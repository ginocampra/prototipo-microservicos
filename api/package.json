{
  "name": "api",
  "version": "0.1.0",
  "scripts": {
    "start": "clear && set NODE_ENV=development && node app",
    "deploy": "clear && set NODE_ENV=production && node app",
    "test": "clear && set NODE_ENV=test && standard"
  },
  "standard": {
    "globals": [
      "CONFIG"
    ]
  },
  "dependencies": {
    "amqp-rpc": "0.0.8",
    "amqp-rpc-factory": "^1.1.4",
    "body-parser": "^1.14.1",
    "express": "^4.13.3",
    "lodash": "^4.17.11",
    "morgan": "^1.6.1"
  },
  "devDependencies": {
    "standard": "^12.0.1"
  }
}
